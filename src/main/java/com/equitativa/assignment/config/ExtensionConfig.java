package com.equitativa.assignment.config;

import org.apache.wicket.protocol.http.WebApplication;
import org.apache.wicket.settings.ResourceSettings;

import com.giffing.wicket.spring.boot.context.extensions.WicketApplicationInitConfiguration;

public class ExtensionConfig implements WicketApplicationInitConfiguration {

	@Override
	public void init(WebApplication webApplication) {
		//ResourceSettings resourceSettings = webApplication.getResourceSettings();
		//resourceSettings.add
		
		
	}

}
